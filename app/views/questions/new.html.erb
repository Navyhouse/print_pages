<div class="container">
    <%# <div class="alert alert-success" role="alert"> %>
    <%# <p><%= notice</p>  %>
    <%# </div> %>
    <%# <div class="alert alert-danger" role="alert"> %>
    <%# <p><%= alert</p> %>
    <%# </div> %>

    <h4 class="mt-3">質問する</h4>
    <div class="p-3 mb-2 bg-light">
      <p>良い回答を得るために</p>
      <p>回答してくれる方がわかりやすいような質問を心がけましょう。</br>
         具体的な事象や画像、既に試した事を記載すると議論にならずにスムーズに回答が得られやすくなります。</br>
         質問するときのヒントも参照ください。</p>
    </div>

    <%= form_with(model: @question, local: true, data: {confirm: "投稿します"}) do |f| %>
      <div class="form-group">
        <%= f.label :title, "質問タイトル" %><br>
        <%= f.text_field :title, class:"form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :body, "質問本文" %><br>
        <%= f.rich_text_area :body, placeholder: 'ここに質問したいことを詳しく書いてください。' %>
      </div>
      <div class="form-group">
        <%= f.label :tag_list, "タグをカンマ区切りで入力" %>
        <%= text_field_tag "question[tag_list]", @question.tag_list.join(","), class:"form-control", data: {role: "tagsinput"} %>
      </div>
      <button type="submit" class="btn btn-dark btn-lg" >投稿</button>
    <% end %>
  
</div>